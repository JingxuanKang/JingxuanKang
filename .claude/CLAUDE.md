# JingxuanKang/JingxuanKang — GitHub Profile README

GitHub 个人资料 README 仓库（与用户名同名，README.md 自动展示在 github.com/JingxuanKang 主页顶部）。

## 红线 / 约定

- 内容真源是 `~/Build/academic-page` 的 `data.js`（bio / news / publications）和 `blog/index.json`（博客列表）。**这里只是镜像**：主页内容变了，手动同步本 README，不要在这里原创内容。
- 博客链接指向 `https://jingxuan.uk/blog/<slug>.html`，不在本仓库存文章。
- 更新流程：改 README.md → commit → `git push`，无部署步骤。
